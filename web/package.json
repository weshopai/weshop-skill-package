{
  "name": "weshop-skill-workspace",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "catalog:check": "node scripts/sync-skill-catalog.mjs && node scripts/check-skill-catalog.mjs",
    "dev": "npm run catalog:check && vite",
    "build": "npm run catalog:check && tsc -b && vite build && node scripts/prepare-sites-worker.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@openai/sites-vite-plugin": "^0.2.0",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "typescript": "latest"
  }
}
